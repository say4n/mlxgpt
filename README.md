# mlxgpt

a small gpt-2-style language model implemented in apple's mlx. the script loads gpt-2 weights through hugging face transformers, maps them into the mlx model, and runs simple greedy text generation.

## run

```sh
uv run model.py
```
